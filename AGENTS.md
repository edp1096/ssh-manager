# 작업 지침 및 대화 인계

이 문서는 Windows 환경에서 후속 대화를 이어가기 위한 맥락이다. 아래 미해결 항목을 자동으로 수정하라는 지시가 아니다. 사용자의 다음 요청에 맞춰 작업한다.

## 프로젝트와 확인된 상태

- 개인용 SSH 호스트 관리자. Go 로컬 HTTP 서버가 Chrome/Chromium/Edge의 앱 창에 HTML/CSS/JS UI를 표시하고, 외부 터미널과 별도 `ssh-client`로 SSH에 연결한다.
- Go workspace: 루트 모듈, `ssh-client`, `tools/archiver`.
- 주요 코드: `internal/server`, `internal/browser`, `internal/host`, `internal/terminal`, `web`.
- 기본 사용 컨셉은 관리 창 하나다.
- 사용자는 ARM64와 x86 계열 장비를 오가며 사용한다. 기존 배포 대상에는 Windows, Linux, FreeBSD가 있다. 실제 유지할 OS/아키텍처 조합은 전환 결정 전에 확인한다.
- 최초 스캔에서 세 모듈의 `go test ./...`, `go vet ./...`는 통과했지만 자동 테스트 파일은 없었다. GUI 실행이나 Windows 재현 검증을 했다는 뜻은 아니다.

## 사용자가 정한 범위와 호환성 조건

- SSH의 `InsecureIgnoreHostKey()`는 연결 시 yes 확인을 피하려는 의도적인 선택이다. 재차 우선 수정 대상으로 밀지 않는다.
- 사용자는 로컬 개인용 앱이라는 맥락에서 웹 보안 지적을 현재 우선순위로 삼지 않는다.
- 저장 파일의 실제 이름은 `hosts.dat`이다. 대화에서 `host.dat`이라고도 언급했다.
- 현행 `hosts.dat` 형식은 유지한다. 나중에 새로운 저장/암호화 방식을 도입하더라도 기존 형식에서 새 형식으로 변환하는 기능이 반드시 필요하다.
- 손상 파일 처리, 인덱스 검증 등 스캔에서 나온 개선 사항은 추후 검토한다.
- 단일 창 컨셉을 감안한다. 다중 창 지원을 임의로 확대하지 않는다.

## GUI 전환 논의

- 아직 전환 프레임워크를 결정하지 않았고 구현 변경도 하지 않았다.
- Wails는 기존 웹 UI를 대부분 재사용하면서 외부 브라우저 탐색, 프로필 삽입, 창 감시를 대체할 후보다. 외부 터미널/tmux나 SSH 기능 자체가 자동으로 개선되는 것은 아니다.
- Wails는 웹뷰 기반이지 OS 기본 위젯 기반 GUI는 아니다. Windows WebView2, Linux GTK/WebKit 의존성과 FreeBSD 지원 여부를 검토해야 한다. 버전 및 지원 상태는 결정 시 공식 문서에서 다시 확인한다.
- 웹뷰 없는 GUI 후보로 Fyne과 Gio를 논의했다. Fyne의 일반 데스크톱 빌드는 CGO와 C 툴체인이 필요하며 `CGO_ENABLED=0` 방식이 아니다. ARM64/AMD64 크로스 빌드는 GOARCH 변경만으로 끝나지 않는다.
- Fyne을 먼저 추천했던 답변은 다중 아키텍처 빌드 편의성을 충분히 반영하지 못했다. Fyne 도입에 합의한 것으로 취급하지 않는다. Gio도 모든 플랫폼에서 CGO 없이 가능하다고 가정하지 않는다.
- 전환한다면 호스트 관리 로직을 HTTP 핸들러에서 분리하고 저장 형식과 외부 터미널/ssh-client를 유지할 수 있다. 터미널 내장은 별도 범위다.

## Windows 브라우저 안내문 문제: 다음 대화의 핵심 맥락

사용자는 Chrome/Edge 앱 창에서 incognito, dev mode 등 각종 안내가 반복되는 것이 싫어 전용 user-data-dir 및 브라우저 데이터 삽입 방식을 사용했다. 사용자는 브라우저가 권하는 앱 방식으로 갈 의사가 없다고 명시했다. 이를 PWA 설치 요청으로 해석하지 않는다.

최근에는 삽입한 데이터에 대해 브라우저 데이터가 변조되었다는 취지의 한 줄 안내가 창 상단에 나타난다고 한다. 이것이 코드/GUI 구조 변경을 고민하게 된 실제 원인이다. 정확한 경고 문구와 브라우저 버전은 아직 확보하지 않았다.

사용자는 user-data-dir 변경과 관련된 문제임을 이미 알고 있다. Windows로 이동해야 하는 새 프로필 재현 실험은 후순위로 두었고, 추가 진단에 매몰되지 말라고 요청했다. 다음 요청 없이 같은 실험이나 프로필 유지 방안을 반복 권하지 않는다.

### 저장소에서 직접 확인한 사실

- `internal/browser/browser.go`는 `--user-data-dir`, `--app`과 여러 안내 억제 옵션으로 브라우저를 실행한다.
- Edge에는 `embeds/browser_data.zip`, Chrome/Chromium에는 `embeds/browser_data.tar.gz`를 풀고 `EditBrowserDataLogins()`를 호출한다.
- 두 압축 파일 모두 `Local State`와 로그인 DB를 포함한다. ZIP 항목에는 2024년 날짜가 보이고, ZIP의 `Local State`에는 `os_crypt.encrypted_key`가 존재한다. 키 값 등 민감한 데이터는 출력하지 않는다.
- `internal/browser/data.go`는 `Default/Login Data`의 URL 관련 레코드를 현재 localhost 주소로 수정한다.
- `internal/server/server.go`의 종료 경로는 브라우저 프로세스를 종료하고 `browser_data`를 삭제한다.
- 사용자는 Local State를 사용하지 않는 것으로 알고 있다고 말했다. Go에서 직접 읽는다는 뜻이 아니라 압축 해제로 배치되어 브라우저가 읽을 수 있다는 점을 설명한 상태다. 실제 Windows 실행 파일/프로필이 저장소와 같은지는 미확인이다.

### 확정하지 않은 것과 이전 답변의 정정

- DPAPI 키의 환경 종속성, 설정 무결성 검사, App-Bound Encryption 등을 가능한 배경으로 언급했지만 이번 경고의 원인을 확정하지 않았다.
- 빈 프로필로 실행하거나 Local State/로그인 DB 배포를 중단하는 것은 진단 및 구성 변경 제안일 뿐, 기존에 억제했던 안내까지 모두 막는 검증된 해결책이 아니다.
- 현재 외부 Chrome/Edge의 `--app` 실행에서 모든 불필요한 안내를 확실히 없애는 검증된 방법은 제시하지 못했다. 만능 플래그나 특정 데이터 삭제로 해결된다고 주장하지 않는다.
- Wails/WebView2는 일반 브라우저의 상단 안내 UI에서 벗어날 후보지만 모든 웹뷰 메시지까지 없다고 보장하지 않는다.
- 사용자의 불편을 해결할 수 있다는 전제로 현 구조 유지를 추천했던 입장은 철회했다. 외부 브라우저의 안내 UI 자체가 전환을 검토할 타당한 이유다.

## 후속 작업 원칙

- 한국어로 응답하고 이미 정한 사용 목적/보류 사항을 존중한다.
- Windows에서 사용자가 문제 진단을 다시 요청하면 실제 안내 문구, 브라우저 버전, 실행 옵션과 사용 중인 프로필을 근거로 좁혀간다.
- 앱 전용 브라우저 프로필과 사용자의 개인 Chrome/Edge 프로필을 구분한다. 개인 프로필이나 전역 브라우저 정책을 임의로 변경하지 않는다.
- 대화 인계 문서 작성 및 `misc.` 커밋/푸시는 완료했다. 브라우저 수정이나 GUI 전환은 아직 승인된 구현 작업이 아니다.

## 후속 승인: Linux 로컬 tmux 대체 실험

- 사용자가 별도 브랜치에서 KDE/Konsole, GNOME/Tilix 분할 구현을 승인했다. 현재 GNOME에 Tilix는 없으므로 설치 안내부터 표시하고, 설치 후 실제 타일링 기준을 시험한다. KDE 환경은 없어 Konsole 실기 시험은 불가능하므로 특히 주의한다.
- `feature/native-terminal-tiling`에서 작업한다. 사용 방법과 검증 상태는 `doc/tiling.md`를 읽는다.
- Linux 전용 변경이다. FreeBSD의 기존 tmux 경로와 Windows Terminal은 유지한다. 자동 테스트 통과와 실제 데스크톱 검증을 구분한다.
- 이후 Tilix 1.9.4가 설치되었다. 잘못 사용한 `--execute` 때문에 실행 실패하던 것을 `-x`로 수정했고 실제 Tilix 창/helper로 생성·분할·그룹 격리 시험을 통과했다. 업그레이드를 요구할 문제가 아니었다. SSH 상호작용 및 Konsole 실기 검증은 아직 남아 있다.
